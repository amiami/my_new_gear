export type Gear = {
  id: string;
  name: string;
  boughtAtDate: string;
  boughtLocation: string;
  comment: string;
  imagePath?: string;
  imageUrl?: string;
  shareId?: string;
  isDisposed: boolean;
  createdAt: string;
};
